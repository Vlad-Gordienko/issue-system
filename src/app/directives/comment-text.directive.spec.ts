import { CommentTextDirective } from './comment-text.directive';

describe('CommentTextDirective', () => {
  it('should create an instance', () => {
    const directive = new CommentTextDirective();
    expect(directive).toBeTruthy();
  });
});
